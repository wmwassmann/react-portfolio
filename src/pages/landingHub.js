import React from 'react'
import { Typography } from '@material-ui/core';

import Typed from "react-typed";

export default function landingHub() {


 
    
    return (
        
        <div>
            <Typography variant="h4">
              <Typed strings={["William Wassmann"]} typeSpeed={40} />
          </Typography>
        </div>
    )
}
